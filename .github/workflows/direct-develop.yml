name: Direct Develop Workflow test

on:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Greeting
        run: echo "Hello Direct Develop World!"

      - name: Print Goodbye
        run: echo "Done - Bye!"
